# UX/UI audit Hadesa — 19. 8. 2026

Orchestrovaný audit piatimi paralelnými read-only agentmi, každý s vlastnou optikou.
Vstup do vlny B a C kontraktu [KONTRAKT-UX-RUNY-CCBRIDGE-2026-08-19.md](../KONTRAKT-UX-RUNY-CCBRIDGE-2026-08-19.md).

| # | Optika | Stav | Plný nález |
|---|---|---|---|
| 1 | Dizajnový systém | ✅ | [audit/01-design-system.md](audit/01-design-system.md) |
| 2 | Čitateľnosť a hustota | ✅ (2. pokus, 20. 8.) | [audit/02-readability.md](audit/02-readability.md) |
| 3 | Informačná architektúra a toky | ✅ | [audit/03-ia-flows.md](audit/03-ia-flows.md) |
| 4 | Prístupnosť | ✅ (2. pokus, 20. 8.) | [audit/04-a11y.md](audit/04-a11y.md) |
| 5 | Parita AI plochy | ✅ | [audit/05-ai-parity.md](audit/05-ai-parity.md) |

Agenti 2 a 4 padli 19. 8. na strop účtu a **pustili sa znova 20. 8., až nad
finálnou paletou**. To nebolo zdržanie, ale podmienka: dovtedy bežal branding
šprint, ktorý prefarbovával akcent na amethyst, takže každé kontrastné číslo by
meralo paletu v pohybe.

Obaja sa kalibrovali (`body` 16,48 tmavá / 15,88 svetlá) a obaja si zapísali pasce,
na ktoré naleteli — bez toho by hlásili falošné pády. Merač kontrastu musí skládať
poloprehľadné vrstvy pozadia a po prepnutí témy nechať dosadnúť; prsteň
`box-shadow` je navyše transitionovaný, takže meranie hneď po `Tab` ukáže celý
rail bez prstenca, hoci ho má.

---

## Tri zistenia, ktoré zmenili plán, nie len jeho položky

**1. `/console` načítava oba stylesheety** (`console.blade.php:22–23`). `mind.css`
teda platí aj na konzole. Duplikáty medzi grafom a konzolou preto nie sú dva
oddelené svety, ale **kaskádový boj v jednom dokumente** — `console.css` utráca
~200 z 818 riadkov na vypínanie základov z `mind.css` (`font: inherit` 7×,
`background: transparent` 9×, `border: 0` 4×). Zjednotenie je tým lacnejšie
a naliehavejšie, než sa plánovalo.

**2. Dáta o behoch už v DB sú.** `console_messages.tokens_in/out`, `duration_ms`,
`stop_reason`, `model` a celá `console_tool_calls`. `RunRecorder` má byť **agregát
nad nimi, nie tretia kópia**. Priamy dôsledok: `AgentRunner.php` — súbor, ktorý
práve prepisuje druhá session — sa nemusí dotknúť vôbec.

**3. Poradie prác v konzole je vynútené.** A16 (strop krokov vyzerá ako hotová
odpoveď), A20 (počítadlo krokov visí po Stope) a A21 (tokeny po obnove zmiznú) sa
**nesmú opravovať pred `RunRecorder`om**: dôvod ukončenia, krok a spotreba musia mať
jeden zdroj. Keby sa napísali najprv na klientovi a potom v recorderi, rozídu sa —
presne to riziko, ktoré kontrakt menuje pri dvojitej ploche.

---

## Nálezy podľa efektu a rizika

### Vysoký efekt, nulové riziko — ide do vlny B alebo C hneď

| ID | Nález | Kam |
|---|---|---|
| **A1** | Konzola nie je z grafu dosiahnuteľná ani jedným klikom | B |
| **A15** | Po obnove stránky konzola vypíše celú systémovú smernicu ako správu Hadesa | B |
| **A17** | „Povoliť vždy" vypne potvrdzovanie natrvalo, ale prepínač v hlavičke to nepovie — **bezpečnostný** | B |
| **A16** | Ukončenie na strope krokov sa tvári ako hotová odpoveď | C (po recorderi) |
| **D6** | `mind.css` nemá `.sr-only` vôbec a `--focus-ring` opakuje 34× per-komponent, kým konzola má jedno globálne `:focus-visible` — **promuje sa konzola do grafu** | B |
| **D2** | `.pc-btn.btn-primary/.btn-ghost/.btn-danger` je hodnota-za-hodnotou to isté ako `button.primary/.ghost/.danger`; jediný volajúci `tools.js:383–385` → 3 riadky JS, −12 r. CSS | B |
| **M-runs** | Log behov neexistuje (potvrdené štvornásobne) | C |

### Stredný efekt

| ID | Nález | Kam |
|---|---|---|
| A2 | `Ctrl+K` → text → `Enter` neotvorí výsledok, ale skočí na Smernicu | B |
| A4 | Denník nemá detail na mieste — každý záznam vyhodí na Graf (2·N klikov) | B |
| A3 | Odpadový label vidí AI, človek ho v UI nenájde | B |
| A11 | Mŕtve: DELETE vlákna, `/model <id>`, 85 vlákien bez správy | B/C |
| A18 | Písanie počas behu je tichý no-op | B (hlásenie, nie front) |
| A12 | Rail: 7 rovnocenných destinácií bez princípu poradia — rastie s Runmi | B |
| A19 | Slash paleta pokrýva demo, nie prácu | B po častiach |
| D1 | `console.css` utráca ~200 r. na vypínanie `mind.css` základov | B |
| D3 | Dva „papiere karty" v jednej appke: graf na `--panel`, konzola na `--surface-2` | B |
| D21 | Konzola nesie nepomenovanú 7-hodnotovú medziškálu (6/7/10/14/18/20/22 px) a 6× píše ikonu ako `14/16/18px`, hoci `--icon-2xs/xs/sm` existujú | B |
| A6 | Zlyhanie `/api/mind` zhodí všetkých 7 obrazoviek | **nie teraz** |

### Nízky efekt

A20 (počítadlo krokov visí), A21 (tokeny po obnove zmiznú), A7 (panel vlákien bez
chybového stavu), A5 (Rozhodnutia sa nedajú hľadať textom), A10 (dok Prehľad
duplikuje Dnes), A9 (mŕtvy chat v grafe duplikuje konzolu), D4 (`kbd` nakreslený 5×
so 4 hodnotami, 3× raw `5px` namiesto `--r-sm`).

---

## Dizajnový systém — čo je zmerané

**Baseline dvojitých deklarácií a farieb** (proti tomuto sa meria kritérium §7/1):

| Súbor | A identické | B iná hodnota | C media/keyframes | raw hex mimo `:root` |
|---|---|---|---|---|
| `mind.css` (743 blokov) | 0 | 4 (menovaná `--card-pad` výnimka, span 17) | 15 | 0 |
| `console.css` | 0 | 4 | 8 | 0 |

Obe tvrdenia z CLAUDE.md sa tým **potvrdili**.

**Tokeny:** 157 (nie 147 — detektor pôvodne prehliadol škálu písanú párovo na
jednom riadku). Mŕtvych 10, z toho 3 vedome. Zmazať sa odporúča len
`--accent-400` (číselný duplikát `--accent-300` v oboch témach) a `--danger-rgb`
(komentár tvrdí volajúcich, ktorí neexistujú).

**Kde stačí alias:** `--card-radius`, `--shadow-*`, `--transition*`,
`--section-gap`, `--page-h1`, `--kpi-value`.

**OPRAVENÉ 20. 8. 2026 — toto tvrdenie bolo nepravdivé.** Audit 1 napísal, že
`.tnum` netreba, pretože to globálne rieši `body { font-variant-numeric }`
(`mind.css:489`), a že 16 lokálnych opakovaní je inertných. Audit čitateľnosti to
zmeral trojkrokovou sekvenciou nad tým istým DOM a vyšlo opačne: **3255 prvkov
v 19 podpisoch malo `font-variant-numeric: normal`** (`.tag.muted` 1728×,
`.record-time` 50×, `.dtl-date` 45×, `.chip-n`…). Príčina: appka kreslí dátové
riadky ako `<button>` a UA `font` shorthand tú deklaráciu z `body` **resetuje**.
Po pridaní `button, input, select, textarea { font-variant-numeric: inherit }`
je to 0 z 0. A obrátene — **3 zo 14 merateľných lokálnych opakovaní nie sú
inertné** (`#pack-count`, `.hero-action .ha-val`, `#rail … .count`), takže ich
zmazanie by čísla rozhýbalo. `font: inherit` sa použiť nedá: `button.record` by
preskočil z 13 na 14 px.

Ten istý koreň má druhý, väčší symptóm: **`line-height: normal` na 13 160
prvkoch** (48 podpisov). `line-height: inherit` ho zruší tiež, ale to je zmena
rozloženia na tisícoch prvkov — patrí do vlny B s vizuálnym overením, nie sem.

**Naozaj chýbajú hodnoty:** `--chart-1..8` (existuje len sekvenčný `--heat-0..4`
a semantický `--cert-*`), `--chart-h*` (výška je dnes v HTML: `mind.blade.php:189
height="60"`), `--row-pad-*` (4 rôzne hodnoty), `--control-h` (32 vs 30 px),
`--stream-w` (`820px` 5× natvrdo). `--card-pad` existuje, ale len na skupine kariet
— do `:root` patrí, inak density prepínač nemá na čom stáť.

---

## Parita AI plochy — dnes ≈ 15 %

MCP má 12 toolov a **všetky sú nad uzlom** (nájdi / prečítaj / zapíš). Ani jeden nie
je ekvivalentom obrazovky.

| Obrazovka | Endpoint | MCP tool | Diera |
|---|---|---|---|
| Dnes | `/api/today` + `/api/dashboard` | čiastočne `mind_overview` | heatmapa, rast, donut istoty, per-area, sync, recent |
| Denník | `/api/journal` | **nie** | AI nemá chronológiu; `mind_recall` nevracia `created_at` |
| Knižnica | `/api/library` | čiastočne `mind_recall` | nevie „všetky skilly v oblasti + `path`" jedným volaním |
| Rozhodnutia | `/api/decisions` | **nie** (`mind_decision` je write-only) | **AI vie rozhodnutie zapísať, nie prečítať** |
| Kontrola | `/api/review/queue` | **nie** (len jedno číslo) | AI frontu plní, nevidí ju |
| Smernica | `/api/directive/build` | **nie** | — |
| Graf | `/api/mind`, `/api/mind/stats` | čiastočne `mind_read` | topológia (váhy hrán) nedostupná |
| Runy | — | — | neexistuje nič |

**Kde sa plochy rozchádzajú už dnes:**

- `smernica.js:209,216–279` — server vracia hotový `markdown`
  (`DirectiveController.php:190`), UI ho **zahodí** a poskladá si vlastný; komentár
  v kóde to priznáva („keď meníš jedno, zmeň aj druhé").
- `dennik.js:63–69` — počty projektov si počíta z 50 načítaných záznamov, hoci
  server posiela `projects{}` za všetky. Server a obrazovka hlásia iné čísla.
- `dennik.js:47–50` + `util.js:354` — skupina „bez projektu" je klientska
  heuristika: AI dostane 12 uzlov `mystifying-mclaren-23750a`, človek vidí jednu
  skupinu.
- `rozhodnutia.js:94,175` — názov oblasti berie z grafového payloadu (`S.areas`),
  lebo `/api/decisions` ho nevracia.
- `McpController.php:470–520` × `MindRecallTool.php:89–120` — **tvar `mind_recall`
  existuje dvakrát a už sa rozišiel** (konzola má `id`, nemá
  `strength/department/verified/origin/semantic`; iné stropy). Plocha je trojitá,
  nie dvojitá. **V tomto šprinte sa nezjednocuje** — `McpController.php` má
  rozpracovaný druhá session.

**Návrh:** `app/Serializers/Screen/*`, endpoint vráti `data()`, MCP tool vráti
`dropEmpty(project(data(), FIELDS_AI))`. Rozdiel plôch je **jeden deklarovaný
zoznam kľúčov**, nie dva kusy kódu.

---

## Čo sa vedome NEROBÍ

1. **Neznižovať počet obrazoviek** (kontrakt §6). Smernicu nezrušiť — A14 ju
   označuje za spornú, ale dôkaz je slabý a telemetria chýba. Kontrolu nezlúčiť.
2. **Nezlučovať Denník s Rozhodnutiami** — dva druhy záznamu, dva dátové zdroje.
   A4 preto zjednocuje **idióm detailu**, nie obrazovky.
3. **Nemazať `chat.js` ani `#prompt`** (A9). Prepnúť prepínač na odkaz do konzoly je
   lacné; odstránenie 398 riadkov s väzbami do `panels.js` je samostatná úloha.
4. **Nemazať Balík pre Claude Code ani `packBtn()`** (A8) — sú na štyroch
   obrazovkách a kým bridge nefunguje, sú to funkčné cesty.
5. **Nerobiť „appku bez grafu"** (A6). `S.byId`/`S.areas` číta priveľa modulov; je to
   nový stav aplikácie, nie príznak, a v šprinte o dizajnovom jazyku by to bola
   najväčšia jediná zmena rizika.
6. **Nerozširovať `/api/search` na rozhodnutia** (A5) — mení tvar odpovede, ktorý
   číta `cmdk.js`. Klientský filter nad 41 už načítanými záznamami dá 95 % úžitku.
7. **Nezavádzať front správ v konzole** (A18) — hlásenie „beh ešte beží" je jeden
   riadok; front je nová funkcia so stavom a má vzniknúť s Runmi.
8. **Nezlučovať `.badge` × `.chip`** (statický vs interaktívny, nesie kánon teal),
   `.card-empty` × `.empty` (zdôvodnené `mind.css:2020`), nepremenúvať tokeny na
   Aura názvy (`--sp-1` má 151 volajúcich).
9. **`verify` sa z MCP nedáva** — overenie poznatku je akt človeka.

---

## Druhá vlna auditov (20. 8. 2026) — čo je opravené a čo čaká

**Opravené hneď** (commit `4a99154`, všetko zmerané pred aj po): `a.ghost` bol
viazaný len na `button`, takže odkaz svietil UA modrou **1,87:1** → 6,05:1. Karta
behu prestala byť `role="button"` s prístupným menom na **778 znakov**; rozbaľuje
skutočné tlačidlo 32×32 s menom na 58 znakov, fokus sa po prekreslení vracia, čipy
dostali `aria-pressed`. Diff a výstup toolu z 11 px na 13 px, prompt behu z 260
znakov na 90ch. Tri konzolové stavy používali surové semantické farby ako text a
padali na svetlej téme (**3,00 / 4,02 / 4,12:1**) → `--warn-ink` / `--danger-ink`.

**Vyvrátené tvrdenie:** `font-variant-numeric` — viď opravu vyššie. Jedna
deklarácia `button, input, select, textarea { font-variant-numeric: inherit }`
znížila 3255 prvkov s `normal` na 0.

### Otvorené — vyžaduje rozhodnutie, nie prácu

**R1 — manuál a CSS majú vymenené role serifu.** `docs/BRAND-HADES.md` §4 hovorí
„hero metriky → Playfair, nadpisy obrazoviek → Geist". Zmerané je to naopak:
`.screen-head h1` je **Playfair 660**, `.hero-val` je **Geist Mono 600**.
`mind.css:2833` to zdôvodňuje vetou „serif je štandard APPIEK rodiny Aura", čo
protirečí hlavičke manuálu („Hades je samostatná značka, nemení sa s Aurou").
Je to rozhodnutie o identite — **neopravovať autonómne**.

**Typografia je celá o stupeň menšia, než tvrdí `--fs-base`.** 85,6 % viditeľného
textu je pod 13 px, z toho 11 px nesie 71,5 %; `--fs-base` (14 px) nesie 31 prvkov
z 13 651. Reálna základná veľkosť Hadesa je 11 px. Bez Knižnice stále 68 % pod
13 px. Zdvihnutie podlahy je zmena vzhľadu celej appky.

**`line-height: normal` na 13 160 prvkoch** (48 podpisov) — ten istý koreň ako
tabulárne čísla. `line-height: inherit` to zruší, ale mení rozloženie na tisícoch
prvkov a chce vizuálne overenie.

**Čipy istoty a riadky diffu** padajú na svetlej téme (4,17 / 4,19 / 4,27 / 4,30:1)
a potrebujú `--cert-*-ink` varianty. **Zámerne neopravené:** syntetická sonda dala
biele pozadie a 21:1, teda merač lhal — a hádať farby z pokazeného merania je
presne to, ako vznikli falošné pády. Treba to zmerať na skutočnom čipe.

**Hustota** @1920×1080 (1036 px pre obsah): Knižnica **20 kariet z 1667**
(23 451 DOM prvkov, 57 776 px scrollu), Kontrola 4 riadky a **733 px nevyužitých**,
Smernica 2 a 546 px, konzolový stream 820 z 1920 px = **42,7 %**.

**Prístupnosť, čo zostáva:** `prefers-reduced-motion` **plátno neutíši** — posun
uzlov 52,10 → 52,14, rAF 290 → 289; ambient utíšený je, `pump()` (`sim.js:213`)
strážený nie je (WCAG 2.2.2). Ďalej: 0 skip linkov a **>400 Tabov** ku composeru;
brána zápisov ohlási nástroj, nie zápis, a po rozhodnutí `#run-announce` mlčí;
`#auto-accept` má cieľ 128×**18** px (pod 860 px 13×13); `canvas#mind` má 1121
uzlov bez prístupnej alternatívy; heatmapa je 365 `div[title]` bez role.

## Pasce, na ktoré audit naletel (aby ich ďalší beh nezopakoval)

- **Detektor raw farieb** hlásil 139 falošných zásahov: telo `:root` prečítané cez
  viacriadkový komentár a `#dec-add-toggle` rozpoznané ako hex `#dec`.
- **Inventár tokenov** videl 8 mŕtvych namiesto 10, pretože škála je písaná **párovo
  na jednom riadku** — a ušli mu presne tie dva, ktoré CLAUDE.md menuje ako vedome
  ponechané.
- **`auth.ui` vracia 401**, takže bez tokenu zo `.env` nie je screenshot ani
  computed style; agent 1 preto tri body označil ako NEOVERENÉ.
