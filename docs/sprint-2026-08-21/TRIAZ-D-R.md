# Triáž auditných nálezov D1–D23 a R1–R11

**Dátum:** 21. 8. 2026 · **Vetva:** `feat/hades-ux` · **HEAD pri triáži:** `3747310`
**Zdroje:** `docs/audit/01-design-system.md` (D1–D23, meraný 19. 8. na `feat/hades-konzola`),
`docs/audit/02-readability.md` (R1–R11, meraný 20. 8. na finálnej amethystovej palete)
**Kontrakt:** `KONTRAKT-UX-APPKA-CHAT-2026-08-21.md`, §4 „Rozsah — čo NIE"

**Metóda:** statické overenie (grep + čítanie kódu) proti aktuálnym súborom.
Prehliadač som nepoužil (Browser pane je vyhradený orchestrátorovi), takže nikde
netvrdím zmerané computed-style číslo, ktoré by som sám nameral — čísla v stĺpci
dôkaz sú z auditov, nové tvrdenia sú výhradne o obsahu súborov.

**Posun súborov od auditu 1:** `mind.css` 4 168 → **4 607** r., `console.css` 818 → **1 073** r.
Riadky z auditu 1 preto nesedia; všetky odkazy nižšie sú prečítané dnes.

**Súhrn:** 34 nálezov · **OPEN 21** · FIXED 8 · WONTFIX 4 · SUPERSEDED 1.

---

## Tabuľka

| ID | Stav | Dôkaz (súbor:riadok) | Efekt | Riziko | Čo presne treba spraviť |
|---|---|---|---|---|---|
| **D1** | **FIXED** (čiastočne, zvyšok v D21) | `console.css:38–74` — jeden pomenovaný reset chrómu pre 12 selektorov, komentár menuje pôvodné počty (7× `font: inherit`, 9× `background: transparent`, 4× `border: 0`). Dnes `font: inherit` **5×** (`console.css:72,179,290,417,908`) a 4 z nich sú mimo resetu (`.rail-find`, `#model-select`, `#prompt`, `.pc-preview` — vlastné prvky, nie duplikáty) | vysoký | — | Nič. `button.bare` sa nepridával, vyriešilo sa to zoznamom selektorov v console.css, čo je lacnejšie a rovnako pomenované. Do kontraktu len zapísať, že smer bol iný než návrh auditu. |
| **D2** | **OPEN — a je to REGRESIA, nie dlh** | CSS strana zaplatená: `console.css:829–848` (komentár „Do 20. 8. 2026 tu bola vlastná trojica"), `.btn-*` v CSS **0 blokov**. Ale `public/js/console/tools.js:383–385` stále emituje `'btn-primary'` / `'btn-ghost'` / `'btn-danger'` a `tools.js:386` skládá `pc-btn ${cls}` | **vysoký** | **nulové** | Tri stringy v `tools.js:383–385` na `primary` / `ghost` / `danger`. Dnes tlačidlá karty potvrdenia zápisu **nemajú žiadny variantný vzhľad** — „Povoliť" nie je akcentové a „Zamietnuť" nie je červené, pretože trieda, na ktorú CSS čaká (`.pc-btn.primary`, `console.css:848`), sa na prvok nikdy nedostane. Toto je najlacnejšia oprava celej triáže a jediná, ktorá dnes mení funkčný stav brány zápisov. |
| **D3** | **OPEN** | `console.css:655–661` `.tool-call` má `background: var(--surface-2)`, `console.css:773–779` `.perm-card` má `background: var(--panel)`; zjednotená karta je `mind.css:4085–4123` (`--card-pad: var(--sp-2)`, `--panel`, `--border`, `--r-md`) a ani jednu z tých dvoch neobsahuje | vysoký | stredné | Rozhodnúť **jeden** papier karty: buď `.tool-call`/`.perm-card` do skupiny na `mind.css:4104` a `--surface-2` len ako pomenovaná varianta (`--card-bg`), alebo naopak. Dnes má appka dva papiere a rozdiel nie je nikde pomenovaný. Vyžaduje preklik toku správ (na tmavej je `--surface-2` svetlejšia než `--panel`). |
| **D4** | **OPEN** (zlepšené o jednu vrstvu) | `console.css:109–119` bare `kbd` (`1px 5px`, `--r-sm`, `--fs-micro`) · `mind.css:1922` `.key-row kbd` · `mind.css:2958` `#cmdk-trigger kbd` · `mind.css:3001` `.cmdk-input-row kbd` · `mind.css:4031` `.kbd-hints kbd` — stále **päť** kresieb | stredný | nízke | Bare `kbd` z `console.css:109` presunúť do `mind.css` (je najúspornejší a platí aj na graf, keďže `console.blade.php` načítava oba stylesheety). `#cmdk-trigger kbd` a `.cmdk-input-row kbd` zmazať celé, `.key-row kbd` / `.kbd-hints kbd` skrátiť na `min-width`/`height`. Overiť, že `5px` radius zmizol (dnes 3 z 5 majú raw `5px`). |
| **D5** | **OPEN** | `mind.css:522` a `console.css:1007` — `.hidden { display: none !important; }` bajt za bajt to isté, oba súbory sa načítavajú spolu (`resources/views/console.blade.php`) | nízky | žiadne | Zmazať `console.css:1007`. |
| **D6** | **OPEN** | `.sr-only` je stále len `console.css:97–107`, v `mind.css` **0 zásahov**. `console.css:91–95` má jedno globálne `.console-body :focus-visible`; `mind.css` má `focus-visible` na **54** miestach (bolo 46) | vysoký | stredné | Promovať `.sr-only` do `mind.css` k `.hidden` (r. 522) a pridať globálne `:focus-visible` pravidlo pre graf. Potom prejsť tých 54 miest a nechať len tie s vlastným `border-radius` (napr. `#brand-core:focus-visible`, `input:focus-visible`). Audit to označil za najlepší pomer efekt/riziko a to platí ďalej — hýbe sa to smerom k už napísanému kódu. |
| **D7** | **OPEN** | `mind.css:553, 561, 3043, 3047` = `.tc-val` / `.tc-label` (today card) · `console.css` má 19 výskytov `.tc-*` (`.tc-head`, `.tc-name`, `.tc-args`, `.tc-state`, `.tc-body`, `.tc-more`, `.tc-caret`). Kolízia stále nie je aktívna, ale ani odzbrojená | stredný | nízke | Premenovať konzolové `.tc-*` na `.tool-*` (karta sa už menuje `.tool-call`) — `public/js/console/tools.js` je jediný volajúci. Alternatíva (`mind.css` → `.today-val`/`.today-label`) je drahšia. |
| **D8** | **OPEN** | `.console-body .msg` prepis žije (`console.css:479`), `#composer #prompt { display: block !important }` žije (`console.css:894–895`, komentár 888–893), `.ms` je v console.css **znova definované celé** (`console.css:1011–1023`, `font-size: 18px` natvrdo) proti `mind.css:524–537` (`--icon-md`), `.hidden` duplikát viď D5 | stredný | — | Kolízie `.msg` a `#prompt` padnú **len naraz s D13** (§9/7 auditu). Čo sa dá spraviť samostatne a bezpečne: `console.css:1011` skrátiť na to, čím sa naozaj líši od `mind.css:524` (dnes je rozdiel len `sans-serif` fallback a `18px` vs `--icon-md`) — ale audit to označil ako NEOVERENÉ bez computed style, takže to potrebuje preklik. |
| **D9** | **OPEN** | `.metric-*` a `.kpi-*` obe rodiny žijú; `.kpi-grid` je `repeat(auto-fit, minmax(160px, 1fr))` natvrdo, `--kpi-cols` neexistuje (0 definícií) | stredný | stredné | `.metric-* → .kpi-*` s modifikátorom `.kpi-card--block` pre dvojriadkovú podobu; `panels.js` + `mind.blade.php` (`#stats-cards`). Povinný preklik panelu Štatistiky. |
| **D10** | **WONTFIX** | Audit 1 §9/1 to výslovne zakazuje a kontrakt §4 to preberá slovo za slovo („Nezjednocovať `.badge` a `.chip`") | — | — | Nerobiť. Reálna časť nálezu (`.tc-state` prepísať na `.badge` pred Runami) je **SUPERSEDED**: obrazovka Runy už existuje a `mind.css:4595–4597` stavia stav behu na `.badge[data-status]`, takže tretia rodina nevznikla. `.tc-state` v konzole zostáva vlastný — je to inline text v hlavičke karty, nie tinta. |
| **D11** | **OPEN** | `.card-empty` `mind.css:2117` · `.empty` `mind.css:2132` · `.empty-state` `console.css:621` (+ `.empty-can` `console.css:636`) — tri prázdne stavy s troma menami | nízky–stredný | nízke | `.empty-state` premenovať na `.empty--hero` (modifikátor `.empty`), `align-items`/`gap` prevziať z `.empty`. `.empty-can` nechať (zoznam schopností, nie prázdny stav). `.card-empty` **nezliať** — viď WONTFIX nižšie. |
| **D12** | **OPEN** (polovica zaplatená) | `public/js/console/slash.js:193` už nastavuje `aria-selected` (a `:166` `id` pre `aria-activedescendant`), ale `slash.js:192` **stále pridáva aj triedu `.on`** a CSS visí na nej: `console.css:990` `.sp-item.on`, `:992` `.sp-item.on .ms` | nízky | nízke | CSS prepnúť na `.sp-item[aria-selected="true"]` a zrušiť `classList.toggle('on', …)` v `slash.js:192`. ARIA diera je už zavretá, zostal duplicitný nosič stavu. |
| **D13** | **OPEN** | `mind.css:1650` `.msg.me`, `:1651` `.msg.hades`, `:1681` `.msg.sys`, `:1688` `.msg.sys--error` × `console.css:510` `.msg.user`, `:519` `.msg.assistant`, `:557` `.msg.system`, `:567` `.msg.error` + `.bubble`/`.who`. `public/js/mind/chat.js` stále existuje (10 561 B) | vysoký | vysoké | Zjednotiť na konzolový slovník (`user`/`assistant`/`system`/`error` + `.bubble`). **Poradie je dané kontraktom §3:** dok Charóna nad grafom najprv funguje, potom sa maže `chat.js` — čím tento nález z väčšej časti **zanikne sám** (graf prestane mať vlastný `.msg` slovník). Nerobiť ako samostatnú CSS vlnu pred dokom; s ním padnú aj obe obchádzky z D8. |
| **D14** | **FIXED / potvrdené** | Pravidlo drží: `console.css` má teraz vlastný `:root` (r. 25–27) s jediným tokenom `--stream-w`, komentár r. 19–24 vysvetľuje prečo. Žiadna literálna farba mimo `:root` som pri čítaní nenašel; hex hodnoty sú len v `:root` blokoch `mind.css` a v favicone (D16) | — | — | Nič. Pozn.: `:root` v `console.css` je nový stav proti auditu (ten meral 0 riadkov `:root`) — detektor `rawcolor2.js` musí odteraz brace-trackovať **oba** súbory, inak by hlásil false-positives. |
| **D15** | **OPEN** | `rgba(var(--accent-rgb), …)` na 8 miestach so 7 hodnotami alfy: `mind.css:715` (.35), `:1184` (.16), `:1186` (.16), `:1188` (.30), `:1189` (.30), `:1572` (.16), `:1577` (.10), `:1602` (.75) | nízky | nízke | Tri pomenované tokeny (`--accent-disabled-fill`, `--accent-focus-wash`, `--accent-text-quiet`), **nie** `--alpha-*` škálu. Kontrakt §3 to menuje ako súčasť rozsahu. Ak sa nerobí, nie je to dlh — pravidlo z CLAUDE.md hovorí o farbe, nie o alfe. |
| **D16** | **OPEN** (hodnoty už správne) | `resources/views/console.blade.php:13` — `%230e1413`, **`%23c4a2f5`** (amethyst, po prefarbení), `%23d8b878`. Hodnoty sa pri prefarbení palety **aktualizovali**, komentár, ktorý menuje tri tokeny, tam ale nie je | nízky | žiadne | Jeden riadok blade komentára nad r. 13: „`--on-accent` dark / `--accent` dark / `--brand-gold` — data-URI SVG nečíta CSS premenné, pri zmene palety opraviť ručne." Nič viac. |
| **D17** | **WONTFIX** | Audit 1 §9/4 a kontrakt §4 („Všetko, čo audity vymenovali v sekciách Čo vedome NEROBIŤ"). `mind.css` A=0 zaplatené v `c1a3a96` | — | — | Nerobiť. Iba znovu spustiť `w4dup.js` ako **akceptačné kritérium** §5/5 na konci šprintu, nie ako prácu. |
| **D18** | **OPEN — zhoršené** | `.empty-state ## max-width`: `console.css:445` `820px` (v skupine s `.tool-call, .perm-card`) × `console.css:621` `600px`. Vzdialenosť bola 113 r., dnes je **176 r.** `#stop ## background`: `console.css:917` `--accent` (skupina s `#send`) × `:930` `--danger`, 13 r. — tesné a čitateľné, nechať | nízky | žiadne | (a) Z `console.css:445` vyňať `.empty-state`, `600px` na r. 621 tým osamostatniť. (b) Do kontraktu §5/5 dopísať baseline `console.css: A=0, B=4, C=8` — dnes tam je len „A=0 v oboch", takže kritérium „žiadna nová dvojica" nemá voči čomu merať B a C. |
| **D19** | **OPEN — potvrdené meraním dnes** | `grep` definícií a `var()` volaní naprieč `public/` + `resources/`: `--muted-strong` 2 def / **0** volaní · `--accent-400` 2/**0** · `--accent-press` 2/**0** · `--glow-accent-lg` 2/**0** · `--success-soft` 2/**0** · `--danger-rgb` 2/**0** · `--z-vignette` 1/**0**. `--accent-400` je aj po prefarbení **presný duplikát** `--accent-300`: `mind.css:127` `#a97ded` = `:128`, `mind.css:430` `#d0b8f8` = `:431` | nízky | nízke | Zmazať `--accent-400` (4 riadky, číselný duplikát) a `--danger-rgb` (jeho komentár na `mind.css:166` tvrdí o kóde niečo, čo v ňom nie je). Ostatným päť **nechať** a dopísať riadok komentára v duchu r. 232 — viď WONTFIX. |
| **D20** | **OPEN** | `--glow-accent` definované `mind.css` (light + dark), jediný volajúci je `console.css:779` (`.perm-card`) | nízky | žiadne | Jeden komentár pri definícii: „jediný volajúci je `.perm-card` v console.css". Chráni to pred zmazaním pri čistení `mind.css`. |
| **D21** | **OPEN** | Ikony stále natvrdo: `console.css:170` (16px), `:500` (14px), `:651` (18px), `:677` (16px), `:793` (18px), `:991` (16px), `:1013` (18px) — sedem miest, kým `--icon-2xs/-xs/-sm` existujú (`mind.css`). Spacing: `10px`×25, `6px`×18, `8px`×10, `12px`×9, `4px`×8, `14px`×5, `5px`×4, `2px`×4, `18px`×3, `7px`×2, `3px`, `20px`, `22px`, `32px` | stredný | nízke (ikony) / stredné (spacing) | (a) Sedem ikonových hodnôt na `--icon-*` — vizuálne inertné, nulové riziko. (b) `4/8/12/16/32 px` na `--sp-*` — inertné, overiť `cssswap.js`. (c) Hodnoty **mimo škály** (5/6/7/10/14/18/20/22) rozhodnúť **naraz s density tokenmi**, nie po jednej — console.css si dnes nesie vlastnú nepomenovanú 8-hodnotovú medziškálu a bez toho density prepínač na konzole nemá čo prepnúť. |
| **D22** | **OPEN** (2 z 3 zaplatené) | Inertné `cursor: default` je **preč** z `.pc-btn` aj `#send` (`console.css:846`, `:929` — už len `opacity`). Zostáva rozdiel hodnôt: `console.css:846` `.pc-btn:disabled { opacity: .5 }` × `:929` `#send:disabled { opacity: .45 }` × `mind.css:704` `button:disabled { opacity: .45 }`; `.perm-card.denied` `.72` (`console.css:784`) | nízky | žiadne | `.5` → `.45` a promovať na token `--disabled-opacity`, aby `.72` na `.perm-card.denied` bola viditeľne **iná rola**, nie tretia náhoda. |
| **D23** | **OPEN** (polovica zaplatená) | `console.css` už má **dva** `prefers-reduced-motion` bloky (`:468–472` pre `.is-new`, `:551–555` pre `.think-dot`/`.sk-row`/`.tr-acts`). Nekryté zostáva: `console.css:714` `animation: tc-pulse 1.1s ease-in-out infinite` a natvrdo časované prechody `console.css:689` (`.12s ease`), `:1042` (`.16s ease`) | stredný | nízke | Do existujúceho bloku na `console.css:551` pridať `.tool-call.running .tc-state { animation: none; }` — je to **jediná nekonečná animácia v appke bez reduced-motion krytia**. Prechody na r. 689 a 1042 na `var(--dur-fast) var(--ease)` (`console.css:241` a `:462` už tak sú, takže je to dorovnanie, nie nový vzor). |
| **R1** | **FIXED** | `mind.css:2887–2896` — `.screen-head h1 { font-family: var(--font) }` s komentárom, ktorý cituje manuál a menuje pôvodné zdôvodnenie ako vyvrátené. `mind.css:3564–3567` — `.hero-val { font-family: var(--serif) }`. `mind.css:547–550` vysvetľuje, prečo `.hero-val` už nie je v mono skupine. Zhoda s `docs/BRAND-HADES.md:227` („Hero metriky → Playfair Display") | — | — | Nič. Role serifu sú vymenené správnym smerom (CSS šlo za manuálom), rozhodnutie bolo vyslovené. Zostáva overiť pri finálnom prekliku, že `.empty.empty-network .title` (tretie serifové miesto, ktoré audit menoval) je zámerné. |
| **R2** | **FIXED** — a tvrdenie bolo **naozaj nepravdivé** | `mind.css:4445–4453`: `button, input, select, textarea { font-variant-numeric: inherit; }` s komentárom, ktorý zapisuje zmerané „3255 prvkov v 19 podpisoch malo `normal`, po tejto deklarácii 0" a výslovne hovorí, že tri z lokálnych opakovaní **nie sú inertné, takže sa NEMAŽÚ** | vysoký | — | Kód je opravený. **CLAUDE.md opravu nepotrebuje** — grep na `tabular`, `font-variant-numeric`, `line-height` v `CLAUDE.md` dáva **0 zásahov**, to tvrdenie v ňom dnes nie je (bolo v audite 1 §5 a v pláne parity). **Ale zostal jeden nepravdivý komentár v kóde:** `mind.css:513–516` stále tvrdí „Jedna deklarácia tu drží celú appku a lokálne opakovania sú od teraz len zbytočné, nie potrebné." To je presne tá veta, ktorú R2 vyvrátil. → **jednoriadková oprava komentára, nulové riziko.** Rovnako `docs/audit/01-design-system.md` §5 a §9/9 ostávajú na papieri ako vyvrátené — nemazať, ale poznamenať. |
| **R3** | **OPEN — tvrdenie platí ďalej** | `line-height: inherit` v `mind.css` aj `console.css`: **0 zásahov**. Koreň je ten istý ako v R2 (UA `font` shorthand na form controls) a R2 dokázal, že ten mechanizmus je reálny — takže 48 podpisov / 13 160 prvkov s `line-height: normal` platí | stredný–vysoký | stredné | Do toho istého bloku `mind.css:4451` dopísať `line-height: inherit`. `font: inherit` **nepoužiť** (zmerané: `button.record` by preskočil z 13 na 14 px). V tom istom kroku prepočítať výšku riadku Denníka a karty Knižnice — riadky sa predĺžia (11 px text: ~13,2 → 16,5 px). Patrí do vlny s density prepínačom, nie ako samostatná záplata. |
| **R4** | **OPEN** (5 z 8 párov zaplatených) | Zaplatené: `console.css:715` `.tool-call.waiting .tc-state → --warn-ink` (komentár „--warn dal na svetlej 3,00:1"), `:716` `.error .tc-state → --danger-ink`, `:567` `.msg.error .who → --danger-ink`. **Zostáva:** `mind.css:3797–3799` `.cert[data-cert="overene"/"hypoteza"/"pasca"]` používajú základné `--cert-*`, a `console.css:765–766` `.diff .df-add` / `.df-del` tiež. `--cert-overene-ink` / `--cert-pasca-ink` neexistujú (0 definícií) | vysoký na svetlej, nulový na tmavej | nízke | Pridať `--cert-overene-ink` a `--cert-pasca-ink` **len do svetlej rampy** (na tmavej nechať alias na základ — tam nepadá nič) a použiť ich v `mind.css:3797`, `:3799` a `console.css:765–766`. Namerané hodnoty dopísať do komentára, presne ako to urobili `--success-ink`/`--warn-ink`/`--danger-ink`. `hypoteza` (4,51) je na hranici — rozhodnúť s ostatnými dvoma. |
| **R5** | **FIXED** | `mind.css:718–723` — komentár menuje nález oboch auditov, pravidlo je `button.ghost, a.ghost { … }` a `button.ghost:hover, a.ghost:hover { … }`. UA `#0000EE` sa už na `a.ghost` nedostane | — | — | Nič. Pri finálnom prekliku overiť aj fokusový prsteň na `a.ghost` (audit meral 1,08:1) — `mind.css:703` je viazané na `button:focus-visible`, takže odkaz ho **nedostáva**; ak sa D6 (globálne `:focus-visible`) urobí, vyrieši to zároveň. **Toto je jediná časť R5, ktorá môže byť ešte otvorená** a je to argument navyše pre D6. |
| **R6** | **OPEN** | Density prepínač neexistuje: `data-density` **0 zásahov** v CSS, JS aj blade. `--row-pad-*`, `--control-h`, `--kpi-cols`, `--chart-*` 0 definícií. `.lib-skill-meta` (`mind.css:2108–2111`) je stále dvojriadkový wrap čipov, `.dtl-card .dtl-head` (`mind.css:3887–3889`) stále `flex` na plnú šírku karty, `.screen-head` (`:2885`) a `.dtl-filter` (`:2418`) sú stále dva samostatné pásy | vysoký | stredné | Poradie podľa ceny za pixel z auditu platí: (a) `.lib-skill-meta` na jeden riadok s „+N" (66 z 151 px karty), (b) `.dtl-head` dvojstĺpcový layout, (c) na Knižnici zlúčiť `.screen-head` + `.dtl-filter` do jedného riadku (−52 px na každej obrazovke s filtrom), (d) density tokeny + `data-density` prepínač. Virtualizácia Knižnice **nie** (WONTFIX). |
| **R7** | **OPEN** (najhoršie miesto zaplatené) | Zaplatené: `mind.css:4575–4585` `.run-diff, .run-result` majú `--fs-body` (13 px) s komentárom, ktorý cituje dôvod. **Zostáva:** `mind.css:4562–4568` `.run-args` a `:4553–4558` `.run-step-who` na `--fs-caption` (11 px), `console.css:710` `.tc-state` 11 px, `console.css:694` `.tc-name` / `:696` `.tc-args` 12 px. Rozpor diff v konzole × diff v Runách je dnes 14 × 13 px (bol 14 × 11) | vysoký | stredné | Podlaha 12 px pre technický výpis, 13 px pre diff/kód. Konkrétne: `.run-args` a `.tc-state` z `--fs-caption` na `--fs-small`; `.run-step-who` nechať (popisok stĺpca, nie dáta) ale rozšíriť stĺpec — `mind.css:4547` `grid-template-columns: 5.5rem 1fr` dáva 88 px = 13 znakov, do čoho sa „assistant" nezmestí. Posledný 1 px rozdiel (13 × 14) doladiť tak, aby diff mal **jednu** veľkosť v celej appke. |
| **R8** | **OPEN** (najhorší pár zaplatený, token chýba) | Zaplatené: `mind.css:4497–4503` `.run-prompt { max-width: 90ch }` s komentárom „260 znakov, 2,9× nad hranicou ~90". **Zostáva:** `--measure` token neexistuje; `.run-error` (`mind.css:4522`), `.dtl-card .dtl-text` (`mind.css:3896`) a `.record-title` bez stropu; `.diff .dl` (`console.css:758`) bez šírkového pravidla | stredný | nízke | Zaviesť `--measure` (78–90ch, hodnota nech sa zhoduje s tou, ktorú si už `.run-prompt` vybral, aby nevznikla druhá pravda) a dať ho na `.run-error`, `.dtl-text`, `.record-title`. Pre kód (`.run-diff`, `.diff .dl`) merať šírkou kódu, nie mierou prózy — tam je 120 znakov v poriadku, ale **musí byť rovnaká veľkosť** (viď R7). |
| **R9** | **OPEN — a je to pasca navyše** | `--stream-w` **vznikol** (`console.css:26`), komentár r. 19–24 hovorí, že „musí zmiznúť ODTIAĽTO, nie vzniknúť druhýkrát" v `mind.css`. **Ale token má nula volajúcich** (`grep var(--stream-w)` = 0) a `820px` je v `console.css` stále **päťkrát** natvrdo: `:445`, `:480`, `:871`, `:933`, `:969` | vysoký | nízke | (a) Päť hodnôt prepnúť na `var(--stream-w)` — inak je token mŕtvy a stav je horší než pred ním (šesť pravd namiesto piatich). (b) Hodnotu potom zmeniť na `min(1200px, 100% - 2 * var(--sp-4))` a pre `.tool-call`, `.diff` a `pre.code` povoliť plnú šírku streamu; próza môže zostať užšia (to je legitímna miera čítania, kód nie). (c) Rozhodnúť, či token zostáva v `console.css:25` alebo sa presúva do `mind.css` `:root` — a ak sa presúva, zmazať ho **odtiaľ**, nie zdvojiť. |
| **R10** | **OPEN** | Podmnožina R6, merané bloky nezmenené: `.rec-grid` (`mind.css:2464`) je `repeat(auto-fill, minmax(min(100%, 640px), 1fr))` → na 1920 dva stĺpce po ~854 px pre 25-znakový titulok; `.lib-skill-meta` (`:2108`), `.dtl-head` (`:3887`), `.run-step` `5.5rem 1fr` (`:4547`) — všetky v pôvodnom tvare | vysoký | stredné | Riešiť spolu s R6. Zvlášť sa vyplatí `.rec-grid` na 3–4 stĺpce (minmax ~420 px) — zdvojnásobí to počet viditeľných riadkov Denníka bez zmeny výšky riadku. Zoznam „vzduch, ktorý je produktívny" (`button.record` 43 px, `.tc-head` 34 px, `.badge`, `#dock`) je **explicitná ochrana**: tie sa nezhusťujú. |
| **R11** | **WONTFIX** | Audit 2 §4/5 to zakazuje bez rozhodnutia; `console.css:235–245` (opacity 0 → hover/focus-within) a `:1058` (dotyk odkrýva natrvalo) sú zámerná dvojica | nízky | — | Nerobiť bez rozhodnutia používateľa. Ak sa rozhodne: `.tr-acts` v pokoji na `opacity: .55`. Zmerané hodnoty pri hoveri (5,16 / 5,28) sú v poriadku, takže to **nie je** a11y nález. |

---

## Dodatočné WONTFIX (audity ich menujú, kontrakt §4 preberá)

| Čo | Zdroj |
|---|---|
| Nezliať `.card-empty` s `.empty` — `mind.css:2113–2117` to už raz rozhodol s dôvodom (nadpis karty kontext povedal, `min-height` drží mriežku dashboardu) | audit 1 §9/2 |
| Neprepisovať Hades tokeny na Aura názvy — len pridávať aliasy (`--sp-1` má stovky volajúcich) | audit 1 §9/3, plán parity §5 |
| Nezjednocovať `.active`/`.current`/`.selected`/`.open` v `mind.css` — `.current` a `.selected` majú 1 volajúceho každý | audit 1 §9/5 |
| Nemazať „mŕtve v škále" tokeny `--muted-strong`, `--accent-press`, `--glow-accent-lg`, `--success-soft`, `--z-vignette` — sú to úplné roly v úplných rebríkoch; diera v škále je horšia než nepoužitý stupeň | audit 1 §9/6 |
| Nesiahať na `.console-body .msg` a `#composer #prompt` izolovane — padnú len naraz s D13 | audit 1 §9/7 |
| Nezavádzať `.tnum` utilitu | audit 1 §9/9 (a R2 dokázal, že správna oprava je `font-variant-numeric: inherit`, nie tretí mechanizmus) |
| Nemeniť farebné hodnoty ani zlatú; `console.css` používa zlatú 0× | audit 1 §9/10, kontrakt §4 |
| Nemeniť farby na tmavej téme — `--*-ink` sú tam zámerne aliasy na základ | audit 2 §4/4 |
| Nerobiť virtualizáciu Knižnice v tomto šprinte (1 667 kariet / 23 451 prvkov je reálne, ale je to nový mechanizmus so stavom) | audit 2 §4/6 |
| Nehlásiť vláskové hrany na plátne ako kontrastnú chybu | audit 2 §4/7, CLAUDE.md |
| Neopravovať R1 autonómne — **už je vyriešené a rozhodnuté**, tento zákaz je vyčerpaný | audit 2 §4/1 |

---

## Poradie prác podľa pomeru efekt/riziko

Zoradené tak, ako sa to dá reálne robiť: najprv to, čo je dnes rozbité, potom to,
čo je čisté pridávanie, potom to, čo hýbe optikou, a nakoniec to, čo potrebuje
vlastný preklik.

### Pásmo 1 — regresie a nulové riziko (robiť hneď, v jednom commite)

| # | Nález | Prečo prvé | Cena |
|---|---|---|---|
| 1 | **D2** — `tools.js:383–385` `btn-* → primary/ghost/danger` | **Dnes to je rozbité.** CSS trojicu zmazal, JS ju stále posiela, takže tlačidlá brány zápisov nemajú variantný vzhľad. Nulové riziko, tri stringy. | 3 riadky JS |
| 2 | **R2-komentár** — `mind.css:513–516` tvrdí niečo, čo `mind.css:4445` vyvrátil | Kód je opravený, komentár klame ďalej a je to presne ten druh vety, na ktorú tento projekt už raz naletel | 1 riadok |
| 3 | **D5** zmazať `console.css:1007` · **D22** `.5 → .45` + `--disabled-opacity` · **D20** komentár k `--glow-accent` · **D16** komentár k faviconu | Mŕtvy a klamlivý kód, nula vizuálnej zmeny | ~8 riadkov |
| 4 | **D19** zmazať `--accent-400` (dokázaný duplikát `--accent-300` v oboch témach) a `--danger-rgb` (jeho komentár klame), zvyšným piatim dopísať riadok „vedome v škále" | Overené dnes grepom, nie prevzaté z auditu | −4 r., +5 komentárov |
| 5 | **D23** doplniť `tc-pulse` do existujúceho reduced-motion bloku (`console.css:551`) a dve časovania na `--dur-fast`/`--ease` | Jediná nekonečná animácia v appke bez krytia; blok, do ktorého to patrí, už existuje | ~4 riadky |
| 6 | **D18** rozviazať `.empty-state ## max-width` (176 r. od seba) + zapísať baseline `console.css: A=0 B=4 C=8` do kontraktu §5/5 | Bez zapísaného čísla nemá akceptačné kritérium voči čomu merať | 2 riadky CSS + 1 riadok kontraktu |
| 7 | **D21a** sedem ikonových `px` na `--icon-*` (`console.css:170, 500, 651, 677, 793, 991, 1013`) | Hodnoty sa nemenia → vizuálne inertné, dokázateľné `cssswap.js` | 7 riadkov |

### Pásmo 2 — vysoký efekt, hýbe sa smerom k už napísanému kódu

| # | Nález | Prečo tu | Riziko |
|---|---|---|---|
| 8 | **D6** promovať `.sr-only` + globálne `:focus-visible` z `console.css` do `mind.css`, potom prečistiť z 54 miest tie bez vlastného radiusu | Najlepší pomer efekt/riziko z auditu 1 a **zároveň dokončuje R5** (fokusový prsteň na `a.ghost` dnes nemá pravidlo) | stredné — treba prejsť všetkých 54 a nechať výnimky |
| 9 | **R9** päť `820px` na `var(--stream-w)`, potom zmeniť hodnotu | Token existuje a má nula volajúcich — dnešný stav je **horší** než pred jeho pridaním | nízke |
| 10 | **R4** dva nové `--cert-*-ink` do svetlej rampy + 4 použitia | Vzor `--success-ink`/`--warn-ink`/`--danger-ink` už funguje (5,13–8,02 na oboch témach), stačí ho dotiahnuť na dva zvyšné páry. Tmavej sa nedotýkať. | nízke |
| 11 | **§5 tokeny** — `--card-pad` do `:root` + aliasy (`--card-radius`, `--shadow-*`, `--transition*`, `--section-gap`, `--page-h1`, `--kpi-value`) a nové hodnoty (`--chart-1..8`, `--control-h`, `--row-pad-*`, `--kpi-cols`, `--measure`) | Iba **pridávanie**, nulové riziko, a je to nosič, na ktorom stojí density prepínač aj R8. Overené dnes: všetkých 11 mien má 0 definícií, `--card-pad` je len lokálny (`mind.css:4107`, `:4123`). | žiadne |
| 12 | **D4** jeden `kbd` základ (5 kresieb → 1 + 2 rozmerové výnimky) | Odstraňuje aj tri raw `5px` radiusy | nízke, −28 r. |
| 13 | **D12** `.sp-item.on` → `[aria-selected="true"]` | ARIA polovica už hotová (`slash.js:193`), zostal duplicitný nosič | nízke |
| 14 | **D7** konzolové `.tc-*` → `.tool-*` | Odzbrojuje nabitú kolíziu; 1 súbor JS | nízke |
| 15 | **D11** `.empty-state` → `.empty--hero` | Tri prázdne stavy na dva legitímne | nízke |
| 16 | **R7** `.run-args`, `.tc-state` na 12 px; diff jedna veľkosť v celej appke; rozšíriť `.run-step` prvý stĺpec | Najhoršie miesto (11 px diff) už zaplatené, zostáva dorovnanie | stredné (mení výšky) |
| 17 | **R8** `--measure` na `.run-error`, `.dtl-text`, `.record-title` | Nadväzuje na `--measure` z bodu 11 | nízke |

### Pásmo 3 — mení optiku, povinný preklik a meranie

| # | Nález | Podmienka |
|---|---|---|
| 18 | **R3** `line-height: inherit` na form controls | Robiť **v tom istom kroku** ako prepočet výšky riadku Denníka a karty Knižnice. 13 160 prvkov zmení výšku riadku. Nikdy nie samostatná záplata. |
| 19 | **D21b/c** spacing na `--sp-*` a rozhodnutie o 8-hodnotovej medziškále `console.css` | Naraz s density tokenmi (bod 11), nie po jednej hodnote |
| 20 | **D3** `.tool-call`/`.perm-card` do zjednotenej karty | Najprv **rozhodnúť** `--panel` vs `--surface-2`; bez toho sa mení dojem celého toku správ |
| 21 | **R6 + R10** hustota: `.lib-skill-meta` → 1 riadok, `.dtl-head` 2 stĺpce, zlúčiť `.screen-head`+`.dtl-filter`, `.rec-grid` 3–4 stĺpce, density prepínač | Stojí na tokenoch z bodu 11. Zoznam „produktívny vzduch" z R10 je ochrana — `button.record` 43 px, `.tc-head` 34 px, `.badge`, `#dock` sa nezhusťujú. |
| 22 | **D9** `.metric-*` → `.kpi-*` + `.kpi-card--block` | Povinný preklik panelu Štatistiky grafu |
| 23 | **D8** dočistiť `.ms` v `console.css:1011` | NEOVERENÉ bez computed style — potrebuje porovnanie s `mind.css:524` v prehliadači |
| 24 | **D13** jeden slovník pre `.msg` (+ obe obchádzky z D8) | **Až po tom, čo dok Charóna nad grafom funguje** (kontrakt §3: „Najprv dok funguje, potom sa maže"). Zmazaním `chat.js` nález z väčšej časti zanikne sám; robiť to pred dokom znamená prepisovať kód, ktorý ide von. |
| 25 | **D15** tri pomenované alfa tokeny | Kozmetika systému; kedykoľvek, nulové riziko, najnižšia priorita |

### Čo z tohto poradia je nové oproti auditu 1 §8

- **D2 vyskočilo na 1. miesto** — v audite bolo #2 ako „duplicita", dnes je to funkčná regresia.
- **D6 zostáva najlepším pomerom** a získalo druhý dôvod (fokusový prsteň `a.ghost` z R5).
- **R9 sa zhoršilo pridaním tokenu**, ktorý nikto nepoužíva — pol opravy je tu horšia než žiadna.
- **D1, D14, R1, R2, R5** sú zaplatené; **D10** zanikol vznikom Rún na `.badge[data-status]`.
- **R2 nevyžaduje opravu CLAUDE.md** — to tvrdenie v ňom nie je. Vyžaduje opravu jedného
  komentára v `mind.css:513–516`, ktorý ho hovorí namiesto neho.
